# ESP-IDF build output
build/
sdkconfig
sdkconfig.old

# Managed components are fetched automatically on first build
# (declared in main/idf_component.yml; versions pinned by dependencies.lock)
managed_components/

# Editor / IDE config (auto-generated by the ESP-IDF VS Code extension,
# contains machine-specific absolute paths)
.vscode/
.clangd
.cache/

# OS / editor cruft
.DS_Store
*.swp
*~
